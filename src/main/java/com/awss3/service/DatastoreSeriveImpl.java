package com.awss3.service;

import java.io.IOException;
import java.util.List;
import java.util.Map;

import org.springframework.beans.factory.annotation.Autowired;
/*
import com.awss3.common.Engine;
import com.awss3.common.Helper;
import com.awss3.enums.RunMode;
import com.awss3.excutore.ExecContext;
import com.awss3.excutore.IExecutor;
import com.awss3.excutore.SparkExcecutor;
import com.awss3.factory.ExecutorFactory;

*/
public class DatastoreSeriveImpl {
	/*
@Autowired 
CommonServiceImpl commonServiceImpl;
@Autowired
ExecutorFactory execFactory;
@Autowired
SparkExcecutor sparkExecutor;
RunMode runMode;
@Autowired
Engine engine;
@Autowired
Helper helper;
	/*
	 @Autowired private SparkExecutor<?> sparkExecut
	 */
/*
	public List<Map<String, Object>> getResult(String type,RunMode mode) {
		List<Map<String, Object>> data = null;
	
		String tableName = getTableNameByDatastore(runMode);
			StringBuilder orderBy = new StringBuilder();
			ExecContext exceContext=commonServiceImpl.getExecContext(mode);
			IExecutor exec = execFactory.getExecutor(exceContext.toString());*/			
	//	data = sparkExecutor.executeAndFetchFromTempTable(
		//		"SELECT * FROM " + tableName;
		
		
		//return null;
//	}
	}



